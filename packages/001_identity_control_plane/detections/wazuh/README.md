# Wazuh — Identity Control Plane Support

Wazuh contributes via endpoint detections (Windows/Linux) that often precede identity abuse:
- suspicious PowerShell / LOLBins
- credential store access patterns
- browser data store access

Add your decoder mappings depending on your telemetry (Sysmon recommended).
